<!-- %h2 Create New User

-# form    action=
= form_tag user_profiles_path do

  -# label "user_name"
  = label :user, :name, 'Name'
  = text_field :user_profile, 'name'

  = label :user, :email, 'Email'
  = email_field :user_profile, 'email'

  = label :user, :occupation, 'Occupation'
  = text_field :user_profile, 'occupation'

  = label :user, :password, 'Password'
  = password_field

  = submit_tag 'Save Changes', :class => 'btn btn-primary'

  = debug(params) -->


  <% provide(:title, 'Sign up') %> <h1>Sign up</h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= form_for(@user_profile) do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>
      <%= f.label :email %>
      <%= f.email_field :email %>
      <%= f.label :password %>
      <%= f.password_field :password %>
      <%= f.label :password_confirmation, "Confirmation" %> <%= f.password_field :password_confirmation %>
      <%= f.submit "Create my account", class: "btn btn-primary" %> <% end %>
    </div>
  </div>
